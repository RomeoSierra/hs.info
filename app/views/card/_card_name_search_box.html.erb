<%= form_tag(index_path, :method => "search_by_class", id: "search-form") do %>
  <%= text_field_tag :search_by_class, params[:search_by_class], placeholder: "Search Cards by Name" %>
  <%= submit_tag "Search", :name => nil %>
<% end %>